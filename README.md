## Lab 4: LLMs and Prompt Engineering for Decision Support

This repo contains Oluwademilade Subair's work from Lab 4

Student ID: 58742028

If you're concerned about reproducability, kindly create your `.env` folder and assign your API key to a variable e.g., `GROQ_API_KEY`.

Then call it using `API_KEY = os.environ["GROQ_API_KEY"]` in part 0

Do install the requirements as well if you haven't already `pip install -r requirements.txt`
